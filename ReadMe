通过反射运行配置文件内容

涉及到的三方：实现某个功能的应用程序；可能需要的各种类（工具类，dao层，service层，controller层很多类）；配置文件

应用程序可能只用到了这些类了的很少，所以我们用反射进行动态加载所需要的类（Class.forName（））。

配置文件作为应用程序和类中间的解耦部分，应用程序更新时，对源码无需进行任何修改，只需要更改配置文件中类的名字。
例如，我们现在需要在应用程序中用Teacher类，只需要在配置文件中该一下类的名字就行；如果要用类中的另一个方法，也是在配置文件中改一下方法的名字即可。

这就是IOC容器的最基本的技术
